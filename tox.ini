[pytest]
addopts = -r chars
		  -v
		  -p no:warnings
		  --capture=no
		  --exitfirst
		  --showlocals
		  --tb=native
filterwarnings =
	ignore:Unknown pytest.mark.AT_
ignore = venv
norecursedirs = venv
